# Dispatch Contracts

Contracts in this folder are product-level references that map to implementation in `/src/contracts/v0.ts`.

Use these files to lock:

- lifecycle states
- legal transitions
- role boundaries
- audit event schema
- closed tool I/O
