# Next Story Recommendation

Canonical backlog source:

- `ai_dispatch_agile_project_package/backlog/backlog.csv`

Next active story recommendation:

- `MVP-08` is complete and recorded as done.
- New active backlog is `ai_dispatch_agile_project_package` v1 commercial rollout and `GLZ-04` is next by dependency and priority.
- For sequencing, finish `Sprint G2` with `GLZ-04`, then move to `GLZ-05`.
